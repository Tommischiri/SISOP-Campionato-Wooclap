```
$ python3 script.py -h
usage: script.py [-h] [-d DIRECTORY] [-pp {fair}] [-sp {bestN}]

Script per calcolare punteggi wooclap

options:
  -h, --help            show this help message and exit
  -d, --directory DIRECTORY
                        Cartella dove si trovano i risultati di ciascuna gara
  -pp, --pointspolicy {fair}
                        Regola di assegnamento punti per ciascuna gara
  -sp, --sumpolicy {bestN}
                        Regola di somma punti per classifica finale
```
